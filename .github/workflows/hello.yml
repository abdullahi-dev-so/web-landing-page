name: Capstone Check

on:
  push:

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check project
        run: |
          echo "Capstone project check passed!"
          echo "Repository files:"
          find . -type f -not -path './.git/*'